int modelwrapper(int param_,np, char modname, int neq, int nlag, int nlead, char eqname_, int eqtype_, int endog_, int delay_, int vtype_) {

  /* antulio.mod_AMA_data(param_, np, modname, neq, nlag, nlead, eqname_, eqtype_, endog_, delay_, vtype_);
   */
    return(0);
}
